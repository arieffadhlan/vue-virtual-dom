services:
    vue-v-dom-explorer:
        image: vue-v-dom-explorer:local
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - '8080:80'
        restart: unless-stopped
